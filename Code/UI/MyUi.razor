@using Sandbox;
@using Sandbox.UI;
@inherits PanelComponent
@namespace Sandbox

<root>
    <div class="vitals">
        <p>🍎 @pg.applesEaten</p>
    </div>
</root>

@code
{
    [Property] SnakeScript pg { get; set; }

    protected override int BuildHash()
    {  
        return System.HashCode.Combine(pg.applesEaten);
    }
}
